# Alpha 2 · CONF-FIX-003 · cumulative inventory correction

This is a **pre-acceptance source checkpoint**, not a claim that the current
commit has passed tests. Post-commit local/head, required localhost PR CI, merge
and separate local exact-main evidence belongs to this packet's PR and retained
operator logs. Those results must bind exact commits and log checksums.

## Authority and scope

- Meta authority: MET-REPAIR-008, PR102, main
  `3b1ea6b9b8514fbb51e37ba4703f5d06298bd938`;
  canonical amendment SHA256
  `491c3ee536b0be231be7f29e3580f357659c7012ec54077268e23aa1f08f48e0`.
- Packet raw SHA256:
  `19089018ab6b1e2dada65d1dc3715940037b0881c1db02217622455a8a91d5a1`.
- Accepted product predecessor: CONF-FIX-002, PR7, main
  `8519225b1564834fab5bcd001c263688e6fba7fe`; 106 tracked files,
  150 test identities, zero skips and seven commands in each predecessor gate.
- Only one existing file changes: the exact three-hunk scalar-test repair,
  resulting SHA256
  `e1491e4407ff6d221871b45bbd775beb28afba11d418ae001847a512bb4b6fe6`.
  Four new owned files contain a test-only helper, twenty regression methods,
  inert fixture and this report. All other predecessor bytes remain fixed.
- `ci/run_packet.py` remains
  `397219b875c040d496edaecaca28bf68725c5338313f047a799235b451ca6de1`;
  the old Linux hash guard remains
  `9111de6b6e167c2eca41801bffc5430b1af8c85f7da696c11a20cce577a1bb29`.
  No runtime/launcher, Makefile, dispatcher, workflow, toolchain, PORTING,
  root helper/policy/key/sudoers or source-lock changes are included.

## Test and source inventory

The fixture pins raw accepted 106-file/150-ID history and separately retains
original 103-file/120-ID bytes. Inert before-source is byte data only: it is
never imported, compiled, evaluated or executed. The fixture checksum is
`44f5dc37ad2ef258302e2454a163dde80ad9350a64f43b290d2049af33b77b86`.

The helper reads actual Git-tracked regular files, rejects conflicted entries,
symlinks, hardlinks, linked ancestors and mode changes, and compares exact
path sets, predecessor SHA256, byte size and mode. Counts are supplementary:

| Source stage | Owning successor | Complete tracked files |
|---|---|---:|
| 0 | CONF-FIX-003 correction | 110 |
| 1 | CONF-LIVE-001 | 120 |
| 2 | CONF-LIVE-002 | 127 |
| 3 | CONF-LIVE-003 | 135 |
| 4 | CONF-LIVE-004 | 141 |
| 5 | CONF-LIVE-005 | 146 |
| 6 | CONF-LIVE-006 | 151 |

Each stage is inferred from its complete ordered path prefix. An environment
flag, matching count, file presence or proof claim cannot exempt changed old
bytes. All new paths must be regular non-executable files. Their membership
is not a code-safety certificate: future packets still require independent
source review, own regression suites, exact CI and merge/main gates.

Fresh child interpreters collect each current suite inside the same signed
offline tree. AST inventories are compared with actual unittest discovery,
source-module identities and pinned 150 predecessor plus twenty repair IDs.
Empty roots, namespace omissions, load_tests filtering, duplicates, skips,
expected failures, deselection and substituted modules refuse. Test bodies
run separately through the declared five-suite argv, not during inventory
collection. The current checkpoint expects **170 actual tests, zero skips**.
Future backend methods stay in their already-declared sixth suite.

The twenty explicitly named tests exercise independent vectors for all seven
stages, every missing/unknown/duplicate/partial/out-of-order path, predecessor
hash/size/mode changes, real filesystem links, fixture tampering, collection
omissions and malformed proof identity/type/size/byte-region constraints.

## Future launcher boundary — source proof only

Stages 0–5 preserve the current launcher exactly and forbid a hook proof.
Stage 6 alone requires one closed string-only, digest-bound proof in
`docs/live-backend/qualification.md`. Only the published terminal unavailable
statement after unchanged manifest verification and preflight may be replaced.
The exact before/prefix/suffix/after hashes and CONF-LIVE-006 packet identity
are checked. Proof, document and indented ASCII replacement sizes are bounded;
duplicate JSON, nonfinite numbers and malformed fence endings refuse.

Synthetic stage-six replacement bytes are never executed. A successful result
is `SOURCE_INVENTORY_ONLY`, `nativeAcceptance=false`; hook proof is
`SOURCE_DELTA_ONLY`, not execution authority or a security certification.
The actual product launcher remains unchanged by this packet.

## Historical evidence remains separate

- CONF-FIX-002 passed 150 tests / seven commands in local/head, required CI
  run `34137197794` / job `101790810481`, and separate local exact-main.
  Its actual PASS is retained; its earlier successor-readiness inference was
  withdrawn by MET-REPAIR-008.
- The unaccepted draft PR6 at
  `57ee9668e029b08310c57f502990e419b4a1c797` remains untouched. Its original
  parser refusal exited 2 before any test or acceptance command; log SHA256
  `604f1ae0a94ed058d0243fd43e0268c5e524ad6a7259e9aa5f0b0b788aa02d39`.
  Queued CI `34115040866` was cancelled without a runner:
  `CANCELLED_NOT_PASS`.
- The later frozen-106 predicate incompatibility is **SOURCE_INSPECTION_ONLY**,
  not a historical product test execution. The new static/unit vector shows
  why adding the original ten stage-one paths yields 116, not 106. With this
  four-file correction included, the accepted complete stage-one set is 120.

## Acceptance and handoff

First signed local candidate `d582425ac6ef9c3ec43984866068fe11a6a2f049`,
activation 67, ran 170 methods with three collection-related errors and zero
skips; exit 1 after the five suites. Campaign/evidence were NOT_RUN by fail-fast.
Log SHA256 `3820936b82948bd335f5d2dfdf0db353a379037b698a853f32225cb88b39df71`.
The fresh collector's `-I` correctly ignored inherited PYTHONPATH but also
omitted the approved local package root. The helper now explicitly adds only
this checkout's checked `src` directory; a collision vector requires a local
package import. No environment isolation, older file or test was weakened.
The corrected candidate still requires a fresh full signed replay.

| Gate | Status at this source publication |
|---|---|
| Source | Five-path implementation checkpoint |
| Local/head offline | WAITING |
| Required localhost PR CI | WAITING |
| Merge | WAITING |
| Separate local exact-main | WAITING |
| Native Linux AMD64/ARM64, installed backend, runtime, tenant | NOT_RUN_ENV_UNAVAILABLE |

Use the unchanged seven direct-argv commands from CONF-FIX-003, empty prefetch,
and installed signed offline launcher in one deny-all-outbound process tree.
Only after all five source gates close may a separate CONF-LIVE-001 run resume
inside its original ten paths. Preserve its six-suite/eight-command acceptance,
all original signed packet bytes and the fresh independent native Linux gate.
No hosted runners, cloud resources, paid APIs, dependencies/runtime downloads,
artifact uploads, warm-source access or renewed administrator installation.

Alpha 2 remains ongoing. No phase-end model-effort transition is due.
